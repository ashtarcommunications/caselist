version: '2.4'
services:
  api:
    image: caselist_client:latest
    container_name: caselist_client
    restart: always
    ports:
        - 5835:5835
    volumes:
        - /net/configs/caselist/client/production.env:/app/.env